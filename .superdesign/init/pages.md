# Pages

## `/` — Starter Counter Page

Entry: `lib/main.dart:MyHomePage`

Dependencies:
- `lib/main.dart`
  - `MyApp` (`MaterialApp` root)
  - `MyHomePage` (`Scaffold`, `AppBar`, `Column`, `FloatingActionButton`)

The existing page is the default Flutter counter screen and does not contain product, navigation, or storefront components. The requested Iron Sam storefront is a new target in the existing codebase.
