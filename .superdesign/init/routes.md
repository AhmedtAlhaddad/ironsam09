# Routes

The starter has no explicit router. `MaterialApp.home` points to `MyHomePage` in `lib/main.dart`.

## Current route map

| URL | Entry | Layout | Summary |
| --- | --- | --- | --- |
| `/` | `lib/main.dart:MyHomePage` | Flutter `Scaffold` | Default counter screen |

The redesign is a new target in this existing Flutter codebase. Planned first customer surface: a responsive storefront home page with product browsing, search/filter affordances, cart access, language switching, and a path toward product detail and checkout.
